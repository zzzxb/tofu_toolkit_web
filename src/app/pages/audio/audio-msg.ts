import {Component, Injectable} from '@angular/core';

@Component({
    selector: 'app-audio-msg',
    templateUrl: './audio-msg.html',
    styleUrls: ['./audio-msg.css']
})

@Injectable( {providedIn: 'root'} )
export class AudioMsg {
}
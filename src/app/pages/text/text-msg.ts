import {Component, Injectable} from '@angular/core';

@Component({
    selector: 'app-text-msg',
    templateUrl: './text-msg.html',
    styleUrls: ['./text-msg.css']
})

@Injectable( {providedIn: 'root'} )
export class TextMsg {
}
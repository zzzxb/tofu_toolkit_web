import {Component, Injectable} from '@angular/core';

@Component({
    selector: 'app-head',
    templateUrl: './head.html',
    styleUrls: ['./head.css']
})

@Injectable( {providedIn: 'root'} )
export class Head {
}